#!/usr/bin/make -f

include /usr/share/cdbs/1/rules/debhelper.mk
include /usr/share/cdbs/1/class/autotools.mk

DEB_CONFIGURE_EXTRA_FLAGS := --disable-static --enable-purify --enable-gtk-doc --with-html-dir=/usr/share/doc/libmatecorba-dev


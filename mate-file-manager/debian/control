Source: mate-file-manager
Section: mate
Priority: optional
Maintainer: Stefano Karapetsas <stefano@karapetsas.com>
Build-Depends: debhelper (>= 5),
               cdbs (>= 0.4.41),
               quilt,
               dpkg-dev (>= 1.13.19),
               libglib2.0-dev (>= 2.24.0),
               mate-desktop,
               libpango1.0-dev (>= 1.1.2),
               libgtk2.0-dev (>= 2.20.0),
               libxml2-dev (>= 2.4.7),
               libexif-dev (>= 0.5.12),
               libexempi-dev (>= 1.99.5),
               libgail-dev (>= 0.16),
               libunique-dev,
               libdbus-glib-1-dev,
               intltool (>= 0.40.1),
               gtk-doc-tools (>= 1.4),
               libxt-dev,
               libx11-dev,
               libselinux1-dev [linux-any],
               libxrender-dev,
               libxext-dev,
               libgconf2-dev,
               x11proto-core-dev,
               shared-mime-info (>= 0.50),
               libstartup-notification0-dev
Standards-Version: 3.9.0

Package: mate-file-manager
Architecture: any
Depends: ${shlibs:Depends},
         ${misc:Depends},
         shared-mime-info (>= 0.50),
         desktop-file-utils (>= 0.7),
         mate-vfs,
         libglib2.0-data,
         gir1.2-mate-file-manager
Description: file manager and graphical shell for MATE, called caja
 Nautilus is the official file manager for the GNOME desktop. It allows
 to browse directories, preview files and launch applications associated
 with them. It is also responsible for handling the icons on the GNOME
 desktop. It works on local and remote filesystems.
 .
 Several icon themes and components for viewing different kinds of files
 are available in separate packages.
 
Package: gir1.2-mate-file-manager
Architecture: any
Depends: ${shlibs:Depends},
         ${misc:Depends},
         shared-mime-info (>= 0.50),
         desktop-file-utils (>= 0.7),
         mate-vfs,
         libglib2.0-data
Description: file manager and graphical shell for MATE, called caja
 Nautilus is the official file manager for the GNOME desktop. It allows
 to browse directories, preview files and launch applications associated
 with them. It is also responsible for handling the icons on the GNOME
 desktop. It works on local and remote filesystems.
 .
 Several icon themes and components for viewing different kinds of files
 are available in separate packages.


